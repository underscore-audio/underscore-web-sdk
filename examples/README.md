# Examples

## hello-world

A single HTML file. No npm, no framework, no build step.
Plays one curated public synth with ~50 lines of vanilla JS.

**Start here.** → [`hello-world/`](./hello-world/)

## backend-proxy

A full Vite + Express example showing the recommended production pattern:
browser holds a publishable key, a small Node proxy holds the secret key
and triggers generation.

→ [`backend-proxy/`](./backend-proxy/)
