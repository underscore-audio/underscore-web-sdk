name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

  e2e:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build SDK
        run: npm run build

      - name: Install example dependencies
        run: cd examples && npm ci

      - name: Copy assets
        run: cd examples && npm run copy-assets

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  wizard:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/wizard
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: packages/wizard/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

  publish-wizard:
    runs-on: ubuntu-latest
    needs: [wizard]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    # id-token: write is required for npm Trusted Publishing via OIDC.
    # contents: read is the default; we declare it explicitly because setting
    # any permission resets all others to none unless listed.
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: packages/wizard
    steps:
      - uses: actions/checkout@v4

      # npm Trusted Publishing requires npm >= 11.5.1. We pin Node 24 (current
      # LTS "Krypton") because it bundles npm >= 11.6.1 out of the box. The
      # previous approach (Node 22 + `npm install -g npm@latest`) is
      # flaky on GitHub hosted runners: the self-upgrade leaves arborist's
      # dependency tree broken (missing `promise-retry`) and the next npm
      # command crashes with MODULE_NOT_FOUND. Staying on a Node version
      # that ships a compatible npm sidesteps the self-upgrade entirely.
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: packages/wizard/package-lock.json
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Check if version changed
        id: check
        run: |
          PUBLISHED_VERSION=$(npm view @underscore-audio/wizard version 2>/dev/null || echo "0.0.0")
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          if [ "$PUBLISHED_VERSION" != "$CURRENT_VERSION" ]; then
            echo "publish=true" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm
        if: steps.check.outputs.publish == 'true'
        run: npm publish --access public --provenance

  publish:
    runs-on: ubuntu-latest
    # e2e is intentionally excluded: the current e2e job invokes a nonexistent
    # `test:e2e` script and has never passed on main. The real end-to-end
    # coverage lives in the main underscore repo (web/tests/e2e/wizard-install.spec.ts)
    # where we can spin up the full API stack. Add a proper e2e here when we
    # have a self-contained live-API fixture worth running on every publish.
    needs: [test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      # See publish-wizard for why we pin Node 24 instead of upgrading npm.
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Check if version changed
        id: check
        run: |
          PUBLISHED_VERSION=$(npm view @underscore-audio/sdk version 2>/dev/null || echo "0.0.0")
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          if [ "$PUBLISHED_VERSION" != "$CURRENT_VERSION" ]; then
            echo "publish=true" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm
        if: steps.check.outputs.publish == 'true'
        run: npm publish --access public --provenance
